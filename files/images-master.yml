---
docker_ceph_version: latest

##########################
# project: ceph

ceph_docker_image: osism/ceph-daemon
ceph_docker_image_tag: "{{ docker_ceph_version }}"
ceph_docker_tag: "{{ docker_ceph_version }}"
