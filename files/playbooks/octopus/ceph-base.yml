---
- name: Deploy base environment
  gather_facts: false
  hosts: "{{ ceph_group_name|default('ceph') }}"
  become: true

  roles:
    - ceph-defaults
    - ceph-facts
    - ceph-handler
    - ceph-container-common
    - ceph-config
    - ceph-mon
    - ceph-mgr
    - ceph-osd
    - ceph-crash
